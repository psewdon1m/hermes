# ===========================================
# P2P Call App - Конфигурация
# ===========================================

# Telegram Bot Configuration
# Получите токен у @BotFather в Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Server Configuration
# Домен для продакшена (например: yourdomain.com)
# Для разработки оставьте localhost
DOMAIN=localhost
PORT=3000

# Server URL Configuration
# URL сервера для API запросов
SERVER_URL=http://localhost:3000

# Redis Configuration
# URL подключения к Redis
REDIS_URL=redis://localhost:6379

# SSL Configuration (for production)
# Пути к SSL сертификатам для HTTPS (Let's Encrypt)
SSL_CERT_PATH=/etc/letsencrypt/live/${DOMAIN}/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/${DOMAIN}/privkey.pem

# TURN Server Configuration
# Настройки TURN сервера для WebRTC
TURN_USERNAME=turnuser
TURN_PASSWORD=turnpass
TURN_SECRET=your-secret-key-here
EXTERNAL_IP=your-server-ip-here
TURN_REALM=yourdomain.com

# Environment Configuration
# Режим работы: development или production
NODE_ENV=development

# Logging Configuration
# Уровень логирования: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ===========================================
# Инструкции по настройке:
# ===========================================
# 1. Скопируйте этот файл в .env: cp env.example .env
# 2. Создайте бота в Telegram через @BotFather
# 3. Скопируйте токен бота в TELEGRAM_BOT_TOKEN
# 4. Для продакшена укажите ваш домен в DOMAIN
# 5. Настройте SSL сертификаты для HTTPS
# 6. Измените пароли TURN сервера для безопасности
# 7. Укажите внешний IP сервера в EXTERNAL_IP
# ===========================================

